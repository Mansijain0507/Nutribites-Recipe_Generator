
import { Navbar } from '../Navbar/Navbar';
// import { Search } from '../search/Search';

function Homee({ theme, setTheme }) {

    return (
        <div>
            <Navbar theme={theme} setTheme={setTheme} />


        </div>
    );
}

export default Homee;